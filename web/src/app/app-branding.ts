/** User-visible product name (browser title, dialogs, onboarding). */
export const APP_DISPLAY_NAME = 'Health Tracker & PFM (Personal Financial Management)';

/** Short name for tight UI (toolbar, abbreviated labels). */
export const APP_SHORT_NAME = 'Health Tracker & PFM';
