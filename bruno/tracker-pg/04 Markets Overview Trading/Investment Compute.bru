meta {
  name: Investment Compute
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/markets/investment-then-now/compute
  body: json
  auth: inherit
}

body:json {
{
  "symbol": "{{symbol}}",
  "startDate": "{{fromDate}}",
  "amount": 10000
}
}
