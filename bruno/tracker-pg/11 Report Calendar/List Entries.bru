meta {
  name: List Entries
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/report-calendar/entries?from={{fromDate}}&to={{toDate}}
  body: none
  auth: inherit
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
}
