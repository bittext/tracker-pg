meta {
  name: Validate US Postal
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/me/address/validate-us-postal?postalCode={{postalCode}}
  body: none
  auth: inherit
}

params:query {
  postalCode: {{postalCode}}
}
