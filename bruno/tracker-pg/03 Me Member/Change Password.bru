meta {
  name: Change Password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/me/password
  body: json
  auth: inherit
}

body:json {
{
  "currentPassword": "{{password}}",
  "newPassword": "{{password}}"
}
}

docs {
Returns a fresh AuthTokenDto — re-save token if rotated.
}
